spring:
    application:
        name: ymq-rabbitmq-spring-boot
    rabbitmq:
        host: localhost
        port: 5672
        username: guest
        password: guest

server:
    port: 8080

mq:
    config:
        exchange: log.direct
